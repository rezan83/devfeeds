import { atom } from "recoil";

export const titleSearchAtom = atom({
    key: "titleSearchAtom",
    default: ""
});

export const categorySearchAtom = atom({
    key: "categorySearchAtom",
    default: ""
});
